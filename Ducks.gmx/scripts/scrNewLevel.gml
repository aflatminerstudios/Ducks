///scrNewLevel(level)

with (objCounter) {
    instance_destroy();
}

with (objGameManager) {
    scrSpawnCounters();
    
    
    ///Set this to be length of opening ditty
    var secs = 1;
    
    alarm[0] = secs * room_speed;
}
